作业1
写一个Python交互的登录系统

思路
首先需要input的输入和判断，
输入的账号时，可以带部分的特殊符号，需要转换字符串，比如，'a_2'
输入密码的时，允许带特殊符号，即为转换成字符串
可以写死，只能指定唯一的用户登录，其他的都拒绝
有登录，和退出的功能，break，exit自由发挥
可以使用字典来进行存储用户信息
还可以添加注册用户的功能
登录欢迎界面等等。。。
eval() 函数可以把json 字符串转为字典！

我写了80行
功能有
判断是否有特殊字符，有则全部过滤
判断输入密码次数不能超过5次，否则强制退出
使用字典做存储用户信息。
使用open函数对文件读写
有登录成功的欢迎界面
有可以添加用户的功能
有判断是否存在已创建用户的功能

